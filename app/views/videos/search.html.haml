%article.video_category
  %header
    .videos.row
      - @results.each do |video|
        .video.col-sm-2
          = link_to video_path(video), nil do
            %img(src="#{video.small_image_url}")
